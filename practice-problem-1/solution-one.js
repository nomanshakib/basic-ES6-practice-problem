
/* 1) Write an arrow function that will take 3 parameters, will multiply
the parameters and will return the result. */

const multiplyThreeNumber = (num1, num2, num3) => num1 * num2 * num3;

/* test */
multiplyThreeNumber(2, 2, 2);
